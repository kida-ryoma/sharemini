.wrapper
  .side_bar
    .form
      = render partial: "form", locals: { post: @post}
    .categories
      = render partial: "category"

  .all-posts
    - @posts.each do |post|
      = render partial: "post", locals: { post: post}

.footer
