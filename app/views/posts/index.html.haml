.header
  = render partial: "menu_bar"

.wrapper
  .form
    = render partial: "form", locals: { post: @post}

  .all-posts
    - @posts.each do |post|
      = render partial: "post", locals: { post: post}

.footer
